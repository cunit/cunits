
if (NOT GOOGLETEST_FOUND) 
    add_library(GOOGLETEST::GOOGLETEST STATIC IMPORTED)
    set(GOOGLETEST_LIBRARY {{cxxpodsLibDir}}/${CMAKE_STATIC_LIBRARY_PREFIX}googletest${CMAKE_STATIC_LIBRARY_SUFFIX})    
    set(GOOGLETEST_LIBRARIES ${GOOGLETEST_LIBRARY})
    set_target_properties(GOOGLETEST::GOOGLETEST PROPERTIES IMPORTED_LOCATION ${GOOGLETEST_LIBRARY})
    
    set(GOOGLETEST_FOUND true)
endif()