
if (NOT gflags_FOUND) 
    set(LIBGFLAGS_INCLUDE_DIR {{cxxpodsIncludeDir}})
    set(LIBGFLAGS_LIBRARY {{cxxpodsLibDir}}/${CMAKE_STATIC_LIBRARY_PREFIX}gflags${CMAKE_STATIC_LIBRARY_SUFFIX})    
    set(LIBGFLAGS_LIBRARIES ${LIBGLOG_LIBRARY})
    
    add_library(LIBGFLAGS::LIBGFLAGS STATIC IMPORTED)
    set_target_properties(LIBGFLAGS::LIBGFLAGS PROPERTIES INCLUDE_DIRECTORIES ${LIBGFLAGS_INCLUDE_DIR})
    set(gflags_FOUND TRUE)
    
endif()