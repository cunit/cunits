
if (NOT FOLLY_FOUND) 
    add_library(FOLLY::FOLLY STATIC IMPORTED)
    set(FOLLY_LIBRARY {{cxxpodsLibDir}}/${CMAKE_STATIC_LIBRARY_PREFIX}folly${CMAKE_STATIC_LIBRARY_SUFFIX})    
    set(FOLLY_LIBRARIES ${FOLLY_LIBRARY})
    set_target_properties(FOLLY::FOLLY PROPERTIES IMPORTED_LOCATION ${FOLLY_LIBRARY})
    
    set(FOLLY_FOUND true)
endif()