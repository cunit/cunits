
if (NOT RSOCKET_FOUND) 
    add_library(RSOCKET::RSOCKET STATIC IMPORTED)
    set(RSOCKET_LIBRARY {{cxxpodsLibDir}}/${CMAKE_STATIC_LIBRARY_PREFIX}rsocket${CMAKE_STATIC_LIBRARY_SUFFIX})    
    set(RSOCKET_LIBRARIES ${RSOCKET_LIBRARY})
    set_target_properties(RSOCKET::RSOCKET PROPERTIES IMPORTED_LOCATION ${RSOCKET_LIBRARY})
    
    set(RSOCKET_FOUND true)
endif()