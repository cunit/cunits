
if (NOT TARGET PNG::PNG) 
    add_library(PNG::PNG STATIC IMPORTED)
    set(PNG_LIBRARIES {{cunitLibDir}}/libpng.a)
    set_target_properties(PNG::PNG PROPERTIES
        IMPORTED_LOCATION {{cunitLibDir}}/libpng.a
    )
    set(PNG_FOUND true)
endif()