
if (NOT TARGET PNG::PNG) 
    add_library(PNG::PNG STATIC IMPORTED)
    set(PNG_LIBRARIES {{cxxpodsLibDir}}/${CMAKE_STATIC_LIBRARY_PREFIX}png${CMAKE_STATIC_LIBRARY_SUFFIX})    
    set_target_properties(PNG::PNG PROPERTIES IMPORTED_LOCATION ${PNG_LIBRARIES})
    set(PNG_FOUND true)
endif()