
if (NOT TARGET libtiff::libtiff) 
    add_library(libtiff::libtiff STATIC IMPORTED)
    set(TIFF_LIBRARY {{cxxpodsLibDir}}/${CMAKE_STATIC_LIBRARY_PREFIX}tiff${CMAKE_STATIC_LIBRARY_SUFFIX})
    set(TIFF_LIBRARIES ${TIFF_LIBRARY})
    set_target_properties(libtiff::libtiff PROPERTIES
        IMPORTED_LOCATION ${TIFF_LIBRARY}
    )
    set(TIFF_FOUND true)
    set(libtiff_FOUND true)
endif()