
if (NOT LIBGLOG_FOUND) 
    add_library(LIBGLOG::LIBGLOG STATIC IMPORTED)
    set(LIBGLOG_LIBRARY {{cxxpodsLibDir}}/${CMAKE_STATIC_LIBRARY_PREFIX}glog${CMAKE_STATIC_LIBRARY_SUFFIX})    
    set(LIBGLOG_LIBRARIES ${LIBGLOG_LIBRARY})
    set(LIBGLOG_INCLUDE_DIR {{cxxpodsIncludeDir}})
    set_target_properties(LIBGLOG::LIBGLOG PROPERTIES IMPORTED_LOCATION ${LIBGLOG_LIBRARY})
    
    set(LIBGLOG_FOUND true)
endif()