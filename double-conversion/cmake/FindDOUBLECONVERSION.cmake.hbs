
if (NOT DOUBLECONVERSION_FOUND) 
    add_library(DOUBLECONVERSION::DOUBLECONVERSION STATIC IMPORTED)
    set(DOUBLECONVERSION_LIBRARY {{cxxpodsLibDir}}/${CMAKE_STATIC_LIBRARY_PREFIX}doubleconversion${CMAKE_STATIC_LIBRARY_SUFFIX})    
    set(DOUBLE_CONVERSION_LIBRARY ${DOUBLECONVERSION_LIBRARY})
    set(DOUBLECONVERSION_LIBRARIES ${DOUBLECONVERSION_LIBRARY})
    set_target_properties(DOUBLECONVERSION::DOUBLECONVERSION PROPERTIES IMPORTED_LOCATION ${DOUBLECONVERSION_LIBRARY})
    
    set(DOUBLECONVERSION_FOUND true)
endif()